<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RestaurantManagementGUI.Views.RevenueReportPage"
             Title="Báo cáo doanh thu"
             BackgroundColor="#F5F5F5">

    <Grid Padding="20" RowDefinitions="Auto,Auto,Auto,*">

        <Grid Grid.Row="0" ColumnDefinitions="Auto,*" Margin="0,0,0,10">
            <Button Grid.Column="0"
                    Text="← Quay lại"
                    BackgroundColor="Transparent"
                    TextColor="#666"
                    FontSize="16"
                    Clicked="OnBackClicked"
                    HorizontalOptions="Start"
                    Padding="0"/>

            <VerticalStackLayout Grid.Column="1" HorizontalOptions="Center">
                <Label Text="Báo Cáo Doanh Thu"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="#263238"
                       HorizontalOptions="Center"/>
                <Label x:Name="DateRangeLabel"
                       Text="Toàn hệ thống - Hôm nay"
                       FontSize="13"
                       TextColor="#999"
                       HorizontalOptions="Center"/>
            </VerticalStackLayout>
        </Grid>

        <Border Grid.Row="1"
                BackgroundColor="White"
                Padding="5"
                Margin="0,10,0,20">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="25"/>
            </Border.StrokeShape>

            <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="5">
                <Button Grid.Column="0"
                        x:Name="DayButton"
                        Text="NGÀY"
                        BackgroundColor="#FFBD59"
                        TextColor="#263238"
                        FontSize="14"
                        FontAttributes="Bold"
                        CornerRadius="20"
                        HeightRequest="40"
                        Clicked="OnDayClicked"/>

                <Button Grid.Column="1"
                        x:Name="WeekButton"
                        Text="TUẦN"
                        BackgroundColor="White"
                        TextColor="#666"
                        FontSize="14"
                        CornerRadius="20"
                        HeightRequest="40"
                        Clicked="OnWeekClicked"/>

                <Button Grid.Column="2"
                        x:Name="MonthButton"
                        Text="THÁNG"
                        BackgroundColor="White"
                        TextColor="#666"
                        FontSize="14"
                        CornerRadius="20"
                        HeightRequest="40"
                        Clicked="OnMonthClicked"/>

                <Button Grid.Column="3"
                        x:Name="YearButton"
                        Text="NĂM"
                        BackgroundColor="White"
                        TextColor="#666"
                        FontSize="14"
                        CornerRadius="20"
                        HeightRequest="40"
                        Clicked="OnYearClicked"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2" 
              ColumnDefinitions="*,*,*" 
              ColumnSpacing="15"
              Margin="0,0,0,20">

            <Border Grid.Column="0"
                    BackgroundColor="White"
                    Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="#E0E0E0" Offset="0,2" Radius="8" Opacity="0.3"/>
                </Border.Shadow>

                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="Auto,*">
                        <Border Grid.Column="0"
                                BackgroundColor="#FFF3E0"
                                WidthRequest="45"
                                HeightRequest="45"
                                HorizontalOptions="Start">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10"/>
                            </Border.StrokeShape>
                            <Label Text="₫"
                                   FontSize="24"
                                   TextColor="#FF9800"
                                   FontAttributes="Bold"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Border>
                    </Grid>

                    <Label Text="Tổng Doanh Thu"
                           FontSize="13"
                           TextColor="#999"/>

                    <Label x:Name="TotalRevenueLabel"
                           Text="0 ₫"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#263238"/>

                    <Grid ColumnDefinitions="Auto,*">
                        <Label Grid.Column="0"
                               x:Name="RevenueTrendLabel"
                               Text="↑ Theo thời gian lọc"
                               FontSize="12"
                               TextColor="#4CAF50"/>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border Grid.Column="1"
                    BackgroundColor="White"
                    Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="#E0E0E0" Offset="0,2" Radius="8" Opacity="0.3"/>
                </Border.Shadow>

                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="Auto,*">
                        <Border Grid.Column="0"
                                BackgroundColor="#E3F2FD"
                                WidthRequest="45"
                                HeightRequest="45"
                                HorizontalOptions="Start">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10"/>
                            </Border.StrokeShape>
                            <Label Text="📋"
                                   FontSize="24"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Border>
                    </Grid>

                    <Label Text="Số Đơn Hàng"
                           FontSize="13"
                           TextColor="#999"/>

                    <Label x:Name="TotalOrdersLabel"
                           Text="0"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#263238"/>

                    <Grid ColumnDefinitions="Auto,*">
                        <Label Grid.Column="0"
                               x:Name="OrdersTrendLabel"
                               Text="↑ Đã thanh toán"
                               FontSize="12"
                               TextColor="#2196F3"/>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border Grid.Column="2"
                    BackgroundColor="White"
                    Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="#E0E0E0" Offset="0,2" Radius="8" Opacity="0.3"/>
                </Border.Shadow>

                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="Auto,*">
                        <Border Grid.Column="0"
                                BackgroundColor="#F3E5F5"
                                WidthRequest="45"
                                HeightRequest="45"
                                HorizontalOptions="Start">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10"/>
                            </Border.StrokeShape>
                            <Label Text="📊"
                                   FontSize="24"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Border>
                    </Grid>

                    <Label Text="TB Giá Trị Đơn"
                           FontSize="13"
                           TextColor="#999"/>

                    <Label x:Name="AverageOrderLabel"
                           Text="0 ₫"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#263238"/>

                    <Grid ColumnDefinitions="Auto,*">
                        <Label Grid.Column="0"
                               x:Name="AverageTrendLabel"
                               Text="↑ Trung bình"
                               FontSize="12"
                               TextColor="#9C27B0"/>
                    </Grid>
                </VerticalStackLayout>
            </Border>
        </Grid>

        <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="20">

            <Border Grid.Column="0"
                    BackgroundColor="White"
                    Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="#E0E0E0" Offset="0,2" Radius="8" Opacity="0.3"/>
                </Border.Shadow>

                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="Auto,*">
                        <Label Grid.Column="0"
                               Text="📊 Biểu đồ Doanh Thu"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#263238"/>
                    </Grid>

                    <ScrollView Orientation="Horizontal" 
                                VerticalOptions="FillAndExpand"
                                HorizontalOptions="FillAndExpand">
                        <Grid x:Name="RevenueChartContainer"
                              RowDefinitions="*,Auto,Auto"
                              ColumnSpacing="8"
                              Padding="10,20,10,10"
                              MinimumWidthRequest="600">

                            <VerticalStackLayout x:Name="EmptyChartState"
                                                 Grid.Row="0"
                                                 Grid.RowSpan="3"
                                                 VerticalOptions="Center"
                                                 HorizontalOptions="Center"
                                                 IsVisible="True">
                                <Label Text="📊"
                                       FontSize="48"
                                       HorizontalOptions="Center"
                                       Opacity="0.3"/>
                                <Label Text="Không có dữ liệu trong khoảng thời gian này"
                                       FontSize="14"
                                       TextColor="#999"
                                       HorizontalOptions="Center"
                                       HorizontalTextAlignment="Center"
                                       Margin="0,10,0,0"/>
                            </VerticalStackLayout>
                        </Grid>
                    </ScrollView>
                </VerticalStackLayout>
            </Border>

            <Border Grid.Column="1"
                    BackgroundColor="White"
                    Padding="20">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="#E0E0E0" Offset="0,2" Radius="8" Opacity="0.3"/>
                </Border.Shadow>

                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="Auto,*">
                        <Label Grid.Column="0"
                               Text="🏆 Hiệu Suất Nhân Viên"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#263238"/>
                    </Grid>

                    <VerticalStackLayout x:Name="EmployeeChartContainer"
                                         VerticalOptions="FillAndExpand"
                                         HorizontalOptions="FillAndExpand"
                                         Padding="20">
                    </VerticalStackLayout>

                    <VerticalStackLayout x:Name="EmptyEmployeeState"
                                         VerticalOptions="Center"
                                         HorizontalOptions="Center"
                                         IsVisible="True">
                        <Label Text="👤" 
                               FontSize="48"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"
                               Opacity="0.3"/>
                        <Label Text="Không có dữ liệu nhân viên"
                               FontSize="14"
                               TextColor="#999"
                               HorizontalOptions="Center"
                               HorizontalTextAlignment="Center"
                               Margin="0,10,0,0"/>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Border>
        </Grid>
    </Grid>
</ContentPage>
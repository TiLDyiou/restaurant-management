<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RestaurantManagementGUI.EditMonAnPage"
             Title="Sửa Món Ăn"
             BackgroundColor="#F5F7FA">

    <ScrollView>
        <VerticalStackLayout Spacing="10" Padding="20">
            <Label Text="Cập nhật thông tin món ăn" FontSize="20" FontAttributes="Bold"/>

            <Label Text="Tên món ăn"/>
            <Entry x:Name="EntryTenMA" />

            <Label Text="Giá tiền"/>
            <Entry x:Name="EntryDonGia" Keyboard="Numeric" />

            <Label Text="Loại"/>
            <Entry x:Name="EntryLoai" />

            <Label Text="Hình ảnh (Link hoặc chọn file)"/>
            <Grid ColumnDefinitions="*, Auto" ColumnSpacing="5">
                <Entry Grid.Column="0" 
                       x:Name="EntryHinhAnh" 
                       Placeholder="Nhập link ảnh hoặc chọn file..." 
                       Unfocused="EntryHinhAnh_Unfocused"
                       Completed="EntryHinhAnh_Completed"/>

                <Button Grid.Column="1" 
                        Text="..." 
                        Clicked="PickImage_Clicked"
                        HeightRequest="45"
                        WidthRequest="50"
                        VerticalOptions="Center" />
            </Grid>

            <Frame BorderColor="Gray" CornerRadius="5" Padding="0" HeightRequest="200" HasShadow="False">
                <Image x:Name="ImgPreview" 
                       Aspect="AspectFit" 
                       BackgroundColor="#EEEEEE"
                       HeightRequest="200" />
            </Frame>

            <Label Text="Trạng thái (true = đang bán, false = nghỉ bán)"/>
            <Switch x:Name="SwitchTrangThai" />

            <Button Text="LƯU THAY ĐỔI" 
                    Clicked="Save_Clicked" 
                    Style="{StaticResource TableStyle_Occupied}" 
                    Margin="0,20,0,0" />

            <Button Text="HỦY" 
                    Clicked="Cancel_Clicked" 
                    Style="{StaticResource TableStyle_Empty}" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>

<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Color x:Key="PrimaryBrandColor">#263238</Color>
    <Color x:Key="SecondaryTextColor">#737373</Color>
    <Color x:Key="ButtonBackground">#FFBD59</Color>
    <Color x:Key="ButtonText">#263238</Color>

    <Style TargetType="Button" x:Key="TableStyle_Empty">

        <Setter Property="TextColor" Value="Black"/>
        <Setter Property="BackgroundColor" Value="#E3F2FD"/>
        <Setter Property="BorderColor" Value="#B0BEC5"/>
        <Setter Property="BorderWidth" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="HeightRequest" Value="50"/>
        <Setter Property="Margin" Value="5,0"/>
        <Setter Property="Padding" Value="15,0"/>
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Brush="#000" Offset="3,3" Radius="8" Opacity="0.1"/>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CategoryButtonStyle" TargetType="Button">
        <Setter Property="BackgroundColor" Value="Transparent" />
        <Setter Property="TextColor" Value="Black" />
        <Setter Property="BorderColor" Value="#DDDDDD" />
        <Setter Property="BorderWidth" Value="1" />
        <Setter Property="CornerRadius" Value="20" />
        <Setter Property="Padding" Value="15,5" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#F0F0F0" />
                            <Setter Property="TextColor" Value="Black" />
                            <Setter Property="BorderWidth" Value="0" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Selected">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="#FF6347" />
                            <Setter Property="TextColor" Value="White" />
                            <Setter Property="BorderWidth" Value="0" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style TargetType="Button" x:Key="TableStyle_Occupied" BasedOn="{StaticResource TableStyle_Empty}">
        <Setter Property="BackgroundColor" Value="#FFD180"/>
        <Setter Property="BorderColor" Value="#E65100"/>
    </Style>

</ResourceDictionary>
<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:conv="clr-namespace:RestaurantManagementGUI.Converters"
             x:Class="RestaurantManagementGUI.UsersPage"
             BackgroundColor="#263238"
             Shell.NavBarIsVisible="True"
             Title="Quản lý nhân viên">

    <ContentPage.Resources>
        <Color x:Key="PrimaryColor">#263238</Color>
        <Color x:Key="AccentColor">#FFBD59</Color>
        <Color x:Key="LightBackground">#FAFAFA</Color>
        <Color x:Key="TextPrimary">#FFFFFF</Color>
        <Color x:Key="TextSecondary">#B0BEC5</Color>

        <conv:StatusColorConverter x:Key="StatusColorConverter"/>
        <conv:CanEditConverter x:Key="CanEditConverter"/>
        <conv:StatusButtonTextConverter x:Key="StatusButtonTextConverter"/>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="18">

            <!-- Tiêu đề nhỏ bên trong -->
            <Label Text="Danh sách nhân viên"
                   FontSize="22"
                   FontAttributes="Bold"
                   TextColor="{StaticResource AccentColor}"
                   HorizontalOptions="Center"/>

            <!-- Nút thêm nhân viên -->
            <Button Text="➕ Thêm nhân viên"
                    BackgroundColor="{StaticResource AccentColor}"
                    TextColor="{StaticResource PrimaryColor}"
                    CornerRadius="12"
                    HeightRequest="50"
                    FontAttributes="Bold"
                    Clicked="OnAddUserClicked" />

            <!-- Danh sách nhân viên -->
            <CollectionView x:Name="UsersCollectionView"
                            SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Stroke="{StaticResource AccentColor}"
                                StrokeThickness="1"
                               StrokeShape="RoundRectangle 14"
                               BackgroundColor="{StaticResource LightBackground}"
                               Padding="15"
                               Margin="0,8">
                            <Border.Shadow>
                                <Shadow Brush="#000" Offset="5,5" Radius="10" Opacity="0.1"/>
                            </Border.Shadow>
                            <VerticalStackLayout Spacing="6">

                                <Label Text="{Binding HoTen, StringFormat='👤 Họ tên: {0}'}"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="#000000" />

                                <Label Text="{Binding MaNV, StringFormat='🆔 Mã nhân viên: {0}'}"
                                       FontSize="14"
                                       TextColor="#333333" />

                                <Label Text="{Binding ChucVu, StringFormat='💼 Chức vụ: {0}'}"
                                       FontSize="14"
                                       TextColor="#444444" />

                                <Label Text="{Binding SDT, StringFormat='📞 Số điện thoại: {0}'}"
                                       FontSize="14"
                                       TextColor="#444444" />

                                <Label Text="{Binding Quyen, StringFormat='🔒 Quyền: {0}'}"
                                       FontSize="14"
                                       TextColor="#757575" />

                                <Label Text="{Binding TrangThai, StringFormat='⚡ Trạng thái: {0}'}"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="{Binding TrangThai, Converter={StaticResource StatusColorConverter}}" />

                                <!-- Nút hành động -->
                                <HorizontalStackLayout Spacing="10" Margin="0,10,0,0">

                                    <!-- Nút Sửa -->
                                    <Button Text="✏️ Sửa"
                                            BackgroundColor="{StaticResource AccentColor}"
                                            TextColor="{StaticResource PrimaryColor}"
                                            CornerRadius="8"
                                            FontAttributes="Bold"
                                            Clicked="OnEditClicked"
                                            IsEnabled="{Binding TrangThai, Converter={StaticResource CanEditConverter}}"
                                            Padding="10,0"/>

                                    <!-- Nút Nghỉ việc / Quay lại -->
                                    <Button Text="{Binding TrangThai, Converter={StaticResource StatusButtonTextConverter}}"
                                            BackgroundColor="#FFA000"
                                            TextColor="White"
                                            CornerRadius="8"
                                            FontAttributes="Bold"
                                            Clicked="OnToggleStatusClicked"
                                            Padding="10,0"/>

                                    <!-- Nút Xóa -->
                                    <Button Text="🗑 Xóa"
                                            BackgroundColor="#E53935"
                                            TextColor="White"
                                            CornerRadius="8"
                                            FontAttributes="Bold"
                                            Clicked="OnHardDeleteClicked"
                                            Padding="10,0"/>
                                </HorizontalStackLayout>

                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:RestaurantManagementGUI.ViewModels"
             xmlns:models="clr-namespace:RestaurantManagementGUI.Models"
             
             xmlns:converters="clr-namespace:RestaurantManagementGUI.Converters"
             
             x:Class="RestaurantManagementGUI.ChefOrdersPage"
             Title="BẾP - NHẬN ĐƠN"
             BackgroundColor="#1E2630">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:TableIdConverter x:Key="TableIdConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto, *">
        <Grid Grid.Row="0" ColumnDefinitions="*, Auto" Padding="20">
            <Label Text="👨‍🍳 ĐƠN HÀNG CẦN CHẾ BIẾN" 
                   FontSize="24" FontAttributes="Bold" TextColor="#FFC107" VerticalOptions="Center"/>

            <Grid Grid.Column="1" WidthRequest="50" HeightRequest="50">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ResetNotificationCountCommand}"/>
                </Grid.GestureRecognizers>

                <Label Text="🔔" FontSize="30" TextColor="White" 
                       HorizontalOptions="Center" VerticalOptions="Center"/>

                <Border BackgroundColor="Red" 
                        StrokeShape="RoundRectangle 10" 
                        StrokeThickness="0"
                        HeightRequest="20" WidthRequest="20"
                        HorizontalOptions="End" VerticalOptions="Start"
                        Margin="0,5,5,0"
                        IsVisible="{Binding HasNewOrders}">
                    <Label Text="{Binding NewOrderCount}" 
                           TextColor="White" FontSize="12" FontAttributes="Bold"
                           HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
            </Grid>
        </Grid>

        <CollectionView Grid.Row="1" ItemsSource="{Binding ActiveOrders}" Margin="10">
            <CollectionView.EmptyView>
                <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                    <Label Text="Hiện chưa có đơn nào..." TextColor="Gray" FontSize="18"/>
                </StackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" Span="2" VerticalItemSpacing="15" HorizontalItemSpacing="15"/>
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:HoaDonModel">
                    <Border StrokeShape="RoundRectangle 15" StrokeThickness="0" BackgroundColor="White" Padding="0">
                        <Grid RowDefinitions="Auto, *, Auto" Padding="15">

                            <Grid Grid.Row="0" ColumnDefinitions="Auto, *" Margin="0,0,0,10">
                                <Border BackgroundColor="#D32F2F" StrokeShape="RoundRectangle 8" Padding="15,5">

                                    <Label Text="{Binding MaBan, Converter={StaticResource TableIdConverter}}" 
                                           TextColor="White" FontAttributes="Bold" FontSize="20"/>

                                </Border>
                                <Label Grid.Column="1" Text="{Binding NgayLap, StringFormat='{0:HH:mm}'}" 
                                       TextColor="Gray" FontSize="16" HorizontalOptions="End" VerticalOptions="Center"/>
                            </Grid>

                            <ScrollView Grid.Row="1" HeightRequest="200" VerticalScrollBarVisibility="Always">
                                <StackLayout BindableLayout.ItemsSource="{Binding ChiTietHoaDons}">
                                    <BindableLayout.ItemTemplate>
                                        <DataTemplate x:DataType="models:ChiTietHoaDonModel">
                                            <Grid ColumnDefinitions="40, *, 40" Margin="0,8">
                                                <Label Text="{Binding SoLuong}" TextColor="#D32F2F" FontAttributes="Bold" FontSize="20" VerticalOptions="Center"/>

                                                <StackLayout Grid.Column="1" VerticalOptions="Center">
                                                    <Label Text="{Binding TenMA}" TextColor="#263238" FontAttributes="Bold" FontSize="18"/>
                                                    <Label Text="{Binding TrangThai}" TextColor="Gray" FontSize="12" FontAttributes="Italic"/>
                                                </StackLayout>

                                                <Button Grid.Column="2" Text="✔" Padding="0" HeightRequest="40" WidthRequest="40" CornerRadius="20"
                                                        BackgroundColor="#2E7D32" 
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ChefOrdersViewModel}}, Path=CompleteDishCommand}"
                                                        CommandParameter="{Binding .}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </BindableLayout.ItemTemplate>
                                </StackLayout>
                            </ScrollView>

                            <Button Grid.Row="2" Text="XONG CẢ BÀN" BackgroundColor="#1E2630" TextColor="White" FontAttributes="Bold" Margin="0,10,0,0"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ChefOrdersViewModel}}, Path=CompleteOrderCommand}"
                                    CommandParameter="{Binding .}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RestaurantManagementGUI.EditChefAndUserProfilePage"
             BackgroundColor="#263238"
             Title="Cập nhật thông tin">

    <ContentPage.Resources>
        <Color x:Key="PrimaryColor">#263238</Color>
        <Color x:Key="AccentColor">#FFBD59</Color>
        <Color x:Key="LightBackground">#FAFAFA</Color>
        <Color x:Key="TextPrimary">#FFFFFF</Color>
        <Color x:Key="TextSecondary">#B0BEC5</Color>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <!-- Số điện thoại -->
            <Label Text="📞 Số điện thoại" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold"/>
            <Entry x:Name="SDTEntry"
                   TextColor="{StaticResource TextSecondary}"
                   BackgroundColor="{StaticResource LightBackground}"
                   Keyboard="Telephone"/>

            <!-- Mật khẩu hiện tại -->
            <Label Text="🔑 Mật khẩu hiện tại" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold"/>
            <Entry x:Name="CurrentPasswordEntry"
                   IsPassword="True"
                   TextColor="{StaticResource TextSecondary}"
                   BackgroundColor="{StaticResource LightBackground}"/>

            <!-- Mật khẩu mới -->
            <Label Text="🆕 Mật khẩu mới" TextColor="{StaticResource TextPrimary}" FontAttributes="Bold"/>
            <Entry x:Name="PasswordEntry"
                   IsPassword="True"
                   TextColor="{StaticResource TextSecondary}"
                   BackgroundColor="{StaticResource LightBackground}"/>

            <!-- Nút Save -->
            <Button Text="💾 Lưu"
                    BackgroundColor="{StaticResource AccentColor}"
                    TextColor="{StaticResource PrimaryColor}"
                    FontAttributes="Bold"
                    CornerRadius="12"
                    HeightRequest="50"
                    Clicked="OnSaveClicked"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
